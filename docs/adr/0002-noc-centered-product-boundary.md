# Keep the product centered on the NoC

The product owns configuration, preview, validation, and generation of exactly one NoC. External IPs may contribute non-generating interface templates for compatibility checks, but they are not configurable or generated objects in the System Design; modeling complete external IPs is reserved for a future context.
