# Show the IP Library only in the creation wizard and developer mode

The normal start screen exposes Recent Designs, New NoC Design, and Open Design without a browsable IP Library. The New NoC Design wizard contains the NoC Package selection step, and developer mode may expose a separate Library/Package inspector. The main workbench never shows the Library or implies that a second NoC can be added.
