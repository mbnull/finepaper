# Let Packages define entities and relations operated through generic Patches

Packages may define additional schema-governed entity and relation types required by an IP Core. User commands, the Default Engine, and Extension Providers modify them using a small generic Patch algebra over stable identities rather than Package-specific core operations; presentation uses generic preview primitives and schema-driven inspectors rather than injected Qt widgets.
