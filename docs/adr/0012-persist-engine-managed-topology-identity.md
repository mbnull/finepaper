# Persist topology identity while keeping topology Engine-managed

Routers, Access Slots, and structural Links are persisted design entities with opaque stable IDs so attachments, diagnostics, and external tools can reference them reliably. Their lifecycle is controlled by the single Package-selected Structure Authority through validated Patches rather than direct user graph editing; the Canvas Preview remains a replaceable projection of these entities.
