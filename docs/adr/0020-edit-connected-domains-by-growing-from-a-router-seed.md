# Edit connected Domains by growing from a Router seed

A connected Domain begins from a user-selected seed Router and grows or shrinks only along structural Link adjacency. The canvas highlights eligible boundary Routers, prevents removals that would split the Domain, and presents membership with fill, border, pattern, and labels rather than color alone; generic connectivity DRC verifies the result independently of the interaction, and changes commit through typed atomic Domain commands.
