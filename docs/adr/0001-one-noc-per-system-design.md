# One NoC per System Design

A System Design contains exactly one NoC, keeping configuration, validation, and generation under one interconnect design boundary. Typed Router Domains are supported inside that NoC; chiplet-specific modeling is deferred and does not justify multiple NoC instances.
