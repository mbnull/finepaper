# Use declarative Packages with optional Extension Providers

Packages describe identity, schemas, interface contracts, and ordinary design capabilities declaratively and rely on the Default NoC Design Engine for common behavior. A Package may additionally supply a versioned Extension Provider only for derivation or preview behavior the common declarations cannot express; Providers operate on domain data and generic preview primitives rather than Qt widgets.
