# Replace the split-state architecture with one DesignSession

The product makes a clean, incompatible transition from the existing split mutable state and undo paths to one authoritative DesignSession, migrated through complete vertical workflows. The core remains explicitly NoC-centered and extensible through schemas, out-of-process Providers, and generic Patches rather than becoming a universal IP platform or continuing to synchronize multiple legacy models.
